export class LogicCupManager {
    static encode(stream) {
        stream.writeBoolean(false)
        stream.writeVInt(7167)
        stream.writeVInt(0)

        stream.writeVInt(0)
        // LogicCupRewards[]
    }
}